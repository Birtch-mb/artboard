{
  "name": "artboard",
  "private": true,
  "scripts": {
    "predev": "lsof -ti :3000 | xargs kill -9 2>/dev/null; lsof -ti :3001 | xargs kill -9 2>/dev/null; exit 0",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.8.12",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@9.15.0"
}
